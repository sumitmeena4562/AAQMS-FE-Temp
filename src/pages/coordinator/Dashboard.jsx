import React from 'react';

const CoordinatorDashboard = () => {
    return (
        <div>
            <h1>Coordinator Dashboard</h1>
            <p>Welcome to the Coordinator workspace. Routing is successfully connected.</p>
        </div>
    );
};

export default CoordinatorDashboard;
