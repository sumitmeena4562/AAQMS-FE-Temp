import React from 'react';
import PlaceholderPage from '../../components/UI/PlaceholderPage';
import { FiTarget } from 'react-icons/fi';

const Zones = () => {
    return <PlaceholderPage title="Zone Management" icon={FiTarget} />;
};

export default Zones;
