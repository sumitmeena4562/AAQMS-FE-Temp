import React from 'react';
import PlaceholderPage from '../../components/UI/PlaceholderPage';
import { FiBox } from 'react-icons/fi';

const Inventory = () => {
    return <PlaceholderPage title="Inventory Management" icon={FiBox} />;
};

export default Inventory;
